{
  "name": "frankrs-mandatory-frameworks",
  "version": "0.3.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0"
  },
  "scripts": {
    "start": "cd express && node app.js",
    "start:dev": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "preinstall": "cd express && npm install",
    "update": "yarn add npm-check-updates --dev && ncu -u && yarn && yarn upgrade && yarn global upgrade && ncu -u && yarn outdated"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "npm-check-updates": "^3.1.8"
  }
}
